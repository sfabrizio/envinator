{
  "name": "PACKAGE_NAME",
  "version": "0.0.0",
  "description": "PACKAGE_DESC",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --coverage",
    "test:watch": "jest --watchAll --coverage",
    "coverage": "nyc report --reporter=text-lcov > coverage.json",
    "lint": "eslint lib test bin index.js",
    "lint:fix": "eslint lib test bin index.js --fix",
    "lint:watch": "npm run lint && onchange lib/*.js test/*.js ./index.js -- npm run lint",
    "precommit": "lint-staged",
    "prepush": "npm test",
    "version": "conventional-changelog -i CHANGELOG.md -s -r 0 && git add CHANGELOG.md"
  },
  "dependencies": {
  },
  "devDependencies": {
    "conventional-changelog-cli": "^1.3.15",
    "eslint": "^4.0.0",
    "husky": "^0.14.3",
    "jest": "^22.4.2",
    "lint-staged": "^6.1.0",
    "nyc": "^11.0.2",
    "onchange": "^3.2.1"
  },
  "engines": {
    "node": "8"
  },
  "lint-staged": {
    "*.js": ["eslint --fix", "git add"]
  },
  "repository": {
    "type": "git",
    "url": "git+https://bitbucket.realtimegaming.com/projects/IT/repos/PACKAGE_NAME.git"
  },
  "keywords": [
    "Games Team"
  ],
  "author": "AUTHOR_NAME",
  "license": "MIT",
  "bugs": {
    "url": "https://bitbucket.realtimegaming.com/projects/IT/repos/PACKAGE_NAME.git/issues"
  },
  "homepage": "https://bitbucket.realtimegaming.com/projects/IT/repos/PACKAGE_NAME.git/readme"
}
